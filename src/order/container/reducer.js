
const key = "order";
const init = {};

const reducer = (state = { init }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { key, init };
export default reducer;
